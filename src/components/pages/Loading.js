import React from 'react';

const Loading = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        width: '100vw',
        height: '100vh',
      }}
    >
      <h1 style={{ fontSize: '3rem' }}>Loading...</h1>
    </div>
  );
};

export default Loading;
